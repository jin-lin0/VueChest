# 知识库文章模板（复制改名后使用）

> 用途：新增知识文档时复制本文件，重命名为 `xxx.md`，并在 `knowledge/index.ts` 追加
> `import xxx from './xxx.md?raw'` 与一条 `{ id: 'xxx', title: '标题', content: xxx }`。
> 文件名即文档 id（**不要带 `kb-` 前缀**，Tab 由所属注册表推导）。

# 标题（一句话说清主题）

导语：本文讲什么、解决什么问题、示例能否直接复制运行（本知识库经 `marked` + `highlight.js` 渲染，代码块务必带语言标签）。

---

## 一、核心概念

用类比 / 表格快速建立心智模型。

| 维度 | A | B |
| ---- | - | - |

## 二、最小可运行示例

```语言标签
// 可复制运行，注释说明关键点
```

## 三、进阶用法

分小节展开，每节「做什么 / 为什么 / 怎么做」。

## N、高频坑速记

| 坑 | 正确做法 |
| -- | -------- |

## 参考来源

- 官方文档（首选）：[名称](https://example.com)
- 权威博客 / 论文 / 库文档

---

写作约定（与现有 13 篇保持一致）：
- 中文、务实、给代码不给空话；每段示例标注「什么时候用」。
- 代码块必须带语言标签（`js` / `ts` / `vue` / `python` / `bash` / `json` / `text`）。
- 结尾必须有「参考来源」段，补权威出处链接（目前是质量打磨重点）。
- 标题层级用 `## 一、二、…`，避免和已有文档结构冲突。
- 不臆造版本号 / 数据；有版本依赖时注明「基于 X 版本」并给官方文档链接供查证。
